<h3>Create a new genre:</h3>
<%= form_for @genre do |f| %>
  <%= f.label :name  %>
  <%= f.text_field :name %>

  <div class="cancel-submit">
    <%= f.submit class: 'btn btn-outline-light' %>
  </div>
<% end %>
