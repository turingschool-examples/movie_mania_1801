<% if !current_user.nil? && current_user.admin? %>
  <%= form_for @genre do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.submit %>
  <% end %>
<% end %>

<% @genres.each do |genre| %>
  <%= genre.name %>
<% end %>
