<main>
  <% if current_user.role == 'admin' %>
    <%= render partial: "form" %>
  <% end %>
<h2>Genres</h2>
  <% @genres.each do |g| %>
    <a href="/genres/<%= g.id %>"><%= g.name %></a>
  <% end %>
</main>
