<% if current_user && current_user.admin? %>
  <%= form_for @genre do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.submit %>
  <% end %>
<% end %>

<% @genres.each do |genre| %>
<%= link_to genre.name, genre_path(genre) %>
<% end %>
